# Desenvolvimento Web em HTML5, CSS3, Javascript e PHP - Projeto Web Node.js

Para instalar as dependências do projeto, execute o comando:

```Bash
npm ci
```

Para executar o servidor da aplicação, execute o comando:

```Bash
npm run dev
```

O servidor HTTP pode ser acessado a partir do endereço **http://localhost:4567**
